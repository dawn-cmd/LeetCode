#include "bits/stdc++.h"
using namespace std;
class Solution {
public:
    void merge(const string &a, const string &b, map<string, string> &fa) {
    }

    vector<double> calcEquation(vector<vector<string>> &equations, vector<double> &values, vector<vector<string>> &queries) {
        map<string, string> f;
        map<string, int> val;
        for (int i = 0; i < equations.size(); ++i) {
        }
    }
};
