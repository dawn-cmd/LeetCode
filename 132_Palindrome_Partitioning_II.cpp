#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    int minCut(string s) {
        vector<vector<int>> dp(s.size(), vector<int>(s.size() + 1, 0x3f3f3f3f));
        for (int i = 0; i < s.size(); ++i) dp[i][1] = 0;
        ahjkasdkljfjkla  
    }
};
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
}

