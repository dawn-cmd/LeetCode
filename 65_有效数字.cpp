#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    bool isNumber(string s) {
        for (int i = 0; i < s.size(); ++i) {
            if (!isdigit(s[i]) && s[i] != e)
        }
    }
};
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
}
